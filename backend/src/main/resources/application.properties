server.port=${PORT:10000}
server.address=0.0.0.0

spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

cors.allowed-origins=${FRONTEND_URL}

logging.level.root=WARN
logging.level.com.copa2026=INFO

# ─── Gmail SMTP ───────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:galofigurinhas@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rtbz hfgd ooly ihrr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# URL base para links de email
app.base-url=${APP_BASE_URL:https://galo-figurinhas-frontend.vercel.app}
